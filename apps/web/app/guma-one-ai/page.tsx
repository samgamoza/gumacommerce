import { Frontend2Landing } from "@/components/marketing/Frontend2Landing";

// Explicit preview of frontend2 (Guma One.ai), regardless of the active-landing toggle.
export const metadata = {
  title: "Guma One.ai — Philippine Social Commerce Engine",
};

export default function GumaOneAiPage() {
  return <Frontend2Landing />;
}
